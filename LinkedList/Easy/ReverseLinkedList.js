const { SinglyLinkedListNode } = require('../../utils/linkedListUtils');

utils = require('../../utils/linkedListUtils');

const reverse = (head) => {
    
}

let head = new utils.SinglyLinkedListNode(1);
utils.printList(head);

